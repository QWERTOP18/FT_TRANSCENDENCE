name: Run Jest tests
runs:
  using: composite
  steps:
    - name: Run Jest tests
      shell: bash
      run: |
        echo "=== Running Jest Tests ==="
        
        echo "Running game service tests..."
        docker compose exec -T game npm test
        
        echo "✅ All tests completed successfully!" 